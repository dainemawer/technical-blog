---
title: How to Determine When a Sticky Element is Stuck in JavaScript
dek: A small JavaScript function for the pseudo-class CSS never gave us.
date: '2024-03-14'
updated: '2026-02-09'
topics:
  - javascript
shortAnswer: CSS has no `:stuck` pseudo-class, so you detect it in JavaScript — cache the element's computed `top` value once, then on a throttled scroll event compare `getBoundingClientRect().top` against it and toggle a class when the two match. Use IntersectionObserver instead only when the sticky element sits at the very top of the page.
takeaways:
  - CSS has no `:stuck` pseudo-class — you have to detect it yourself in JavaScript.
  - Cache `getComputedStyle` and the parsed `top` value once; recomputing them on every scroll event is expensive and unnecessary.
  - Throttle the scroll listener to around 200ms. It doesn't hurt the user experience and meaningfully reduces main-thread work.
  - IntersectionObserver is more performant, but only works cleanly when the sticky element is the very first thing on the page.
  - "classList.toggle()'s second argument (force) turns it into a one-way operation — useful for this exact pattern."
faq:
  - question: Why doesn't CSS have a `:stuck` pseudo-class?
    answer: The `position:sticky` spec only describes behavior, not an inspectable state — there's no native selector for whether a sticky element is currently stuck, so detecting it has to happen in JavaScript.
  - question: Can I use IntersectionObserver to detect a stuck element instead?
    answer: Yes, but only reliably when the sticky element is the first thing on the page — it needs a -1px top offset to trigger at the right moment. If the sticky element sits further down the page, behind another sticky header for example, that hack doesn't hold up, and the scroll-based approach below is more robust.
  - question: Does listening to the scroll event hurt performance?
    answer: Not if you throttle it. Scroll fires very frequently, but capping execution to once every 200ms, as in the code below, keeps the main thread free without any perceptible lag for the user.
needsRewrite: false
---

`position: sticky` is a handy CSS positioning property that sticks an element to the top of its relative parent on scroll. It's a considerable step up from the older approach of tracking a scroll offset and manually switching an element to `position: fixed`.

## Why CSS Can't Detect a Stuck Element

CSS has no way to query whether a sticky element is currently stuck. That's frustrating, because sticky headers often need a different look once they've stuck — a shadow, a smaller logo, a background colour. A `:stuck` pseudo-class would solve this outright, but the spec doesn't define one.

The fix is a small JavaScript function that watches the element's position and toggles a class when it's stuck:

```javascript
let stickyElementStyle = null;
let stickyElementTop = 0;

function determineStickyState(element) {
  if (!stickyElementStyle) {
    stickyElementStyle = window.getComputedStyle(element);
    stickyElementTop = parseInt(stickyElementStyle.top, 10);
  }

  const currentTop = element.getBoundingClientRect().top;

  element.classList.toggle('is-sticky', currentTop <= stickyElementTop);
}

window.addEventListener('scroll', throttle(determineStickyState, 200));
```

## How the Function Works

`determineStickyState` takes the sticky element and is bound to the window's `scroll` event through a `throttle` helper, which caps execution to once every 200ms. Scroll fires far more often than that, and without throttling, this function would run on every single delta — costly for the browser, with no benefit to the user.

The last line does the actual work: it toggles a class called `is-sticky` if `currentTop` is less than or equal to `stickyTop`. Fewer engineers know that `classList.toggle()` accepts a second `force` argument — passing `true` or `false` turns it into a one-way operation instead of a toggle, which is exactly the stateful behaviour this pattern needs. Per MDN:

> If included, it turns the toggle into a one-way-only operation. If set to false, the token will only be removed but not added. If set to true, the token will only be added but not removed.

The two unscoped variables, `stickyElementStyle` and `stickyElementTop`, exist purely to cache the expensive part. `window.getComputedStyle` is costly to call on every scroll event, so it only runs once, the first time the function fires; after that, the cached `top` value (parsed to a number via `parseInt`) is reused.

The comparison itself relies on two different values that are easy to conflate: `getBoundingClientRect().top` returns the element's *current* rendered position, while `getComputedStyle().top` returns the *CSS-defined* top value the element is sticking to. Once the current position reaches or passes that defined value, the element is stuck.

## Why IntersectionObserver Doesn't Work Here

IntersectionObserver would be the more performant choice — it avoids a scroll listener entirely. The catch is that detecting a sticky element with it requires a hack: setting the element's `top` to `-1px` so the observer fires at the right threshold. That only holds up when the sticky element is the very first thing on the page.

In this case it wasn't. The sticky element sat further down the page, behind an already-sticky site header, so its effective `top` needed to be a positive number that accounted for that header's height — which the IntersectionObserver hack can't express. The scroll-and-throttle approach above works regardless of where the element sits.

The full code is available as a [Gist](https://gist.github.com/dainemawer/e233dc5b3ea82caa3a984cf34c6b339f).
